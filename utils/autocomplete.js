export let styles = {
  item: {
    padding: '2px 6px',
    cursor: 'default'
  },

  highlightedItem: {
    color: 'white',
    background: 'hsl(200, 50%, 50%)',
    padding: '2px 6px',
    cursor: 'default'
  },

  menu: {
    border: 'solid 1px #ccc'
  }
}


export function matchElementToTerm (e, value) {
  return (
    e.name.toLowerCase().indexOf(value.toLowerCase()) !== -1 ||
    e.abbr.toLowerCase().indexOf(value.toLowerCase()) !== -1
  )
}

export function sortElements (a, b, value) {
  return (
    a.name.toLowerCase().indexOf(value.toLowerCase()) >
    b.name.toLowerCase().indexOf(value.toLowerCase()) ? 1 : -1
  )
}

